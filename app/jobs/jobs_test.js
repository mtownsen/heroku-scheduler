'use strict';

describe('myApp.jobs module', function() {

  beforeEach(module('myApp.jobs'));

  describe('jobs controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var jobsCtrl = $controller('JobsCtrl');
      expect(jobsCtrl).toBeDefined();
    }));

  });
});